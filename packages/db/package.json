{
  "name": "@tyl/db",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    "./server": {
      "types": "./dist/server/index.d.ts",
      "default": "./src/server/index.ts"
    },
    "./server/*": {
      "types": "./dist/server/*.d.ts",
      "default": "./src/server/*.ts"
    },
    "./client/*": {
      "types": "./dist/client/*.d.ts",
      "default": "./src/client/*.ts"
    },
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf .turbo node_modules",
    "lint": "oxlint .",
    "generate": "pnpm with-env drizzle-kit generate",
    "generate:manual-migration": "pnpm with-env drizzle-kit generate --custom",
    "up": "pnpm with-env drizzle-kit up",
    "migrate": "pnpm with-env drizzle-kit migrate",
    "migrate:deploy": "pnpm drizzle-kit migrate",
    "push": "pnpm with-env drizzle-kit push",
    "studio": "pnpm with-env drizzle-kit studio",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@powersync/common": "catalog:powersync",
    "@powersync/drizzle-driver": "catalog:powersync",
    "@tanstack/powersync-db-collection": "catalog:tanstack",
    "drizzle-orm": "catalog:",
    "drizzle-zod": "^0.8.3",
    "uuid": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@tyl/tsconfig": "workspace:*",
    "@types/pg": "^8.16.0",
    "dotenv-cli": "catalog:",
    "drizzle-kit": "catalog:",
    "pg": "^8.17.2",
    "typescript": "catalog:"
  }
}