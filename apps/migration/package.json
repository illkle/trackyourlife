{
  "name": "@tyl/migration",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "start": "node dist/index.cjs",
    "dev": "pnpm with-env tsup --watch --onSuccess \"node dist/index.cjs\"",
    "build": "tsup",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@tyl/db": "workspace:*"
  },
  "devDependencies": {
    "@tyl/tsconfig": "workspace:*",
    "tsup": "^8.5.1",
    "typescript": "catalog:"
  }
}
